// Always include this instead of the global "Common.hlsl" for Dying Light shaders,
// so that game-specific cbuffer definitions (GameCBuffers.hlsl) are picked up first.

#include "GameCBuffers.hlsl"
#include "../../Includes/Common.hlsl"
